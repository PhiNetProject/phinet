{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ΦNET desktop — shell open, file dialog, and fs read for the vault import.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:allow-open",
    "fs:default",
    "fs:allow-read-file"
  ]
}
